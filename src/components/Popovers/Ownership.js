import React from "react";
import Popover from "react-bootstrap/Popover";
const OwnershipPopover = (
  <Popover id="OwnershipPopover">
    <Popover.Content>
      Player's ownership % in their team's last single game competition they
      played in
    </Popover.Content>
  </Popover>
);

export default OwnershipPopover;
