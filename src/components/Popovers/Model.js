import React from "react";
import Popover from "react-bootstrap/Popover";
const ModelPopover = (
  <Popover id="ModelPopover">
    <Popover.Content>
      What the player's odds should be according to our predictive model based
      on 7+ years worth of player & team data
    </Popover.Content>
  </Popover>
);

export default ModelPopover;
