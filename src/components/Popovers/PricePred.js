import React from "react";
import Popover from "react-bootstrap/Popover";
const PricePredPopover = (
  <Popover id="PricePredPopover">
    <Popover.Title as="h3">Best Odds</Popover.Title>
    <Popover.Content>
      Our assessment of the player's value - lower the number, the better value
      they are
    </Popover.Content>
  </Popover>
);

export default PricePredPopover;
